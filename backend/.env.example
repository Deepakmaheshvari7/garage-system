# Copy this file to .env and fill in real values for local development.
# In production (Render/Heroku), set these as native environment variables
# in the platform dashboard instead of using a .env file.

ENV=development

# PostgreSQL connection string
DATABASE_URL=postgresql://garage_user:garage_pass@localhost:5432/garage_db

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=9257b73dae38448fc17678ea212522ac9cc07f01b773aed84e31ad31db614528
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Business settings
STANDARD_LABOR_RATE=500.0
TAX_PERCENTAGE=18.0

# CORS — set to your deployed Streamlit URL in production
FRONTEND_ORIGIN=http://localhost:8501
